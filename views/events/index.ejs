
<% layout('layouts/app') %>  

<div class="min-h-screen bg-gray-50">
<!-- Header -->
<div class="bg-blue-600 text-white py-4">
  <div class="container mx-auto flex justify-between items-center px-4">
    <a href="/events" id="tombol-kembali" class="text-white flex items-center gap-2">
      &larr; Kembali ke Beranda
    </a>
    <h1 class="text-2xl font-bold">Event di Indonesia</h1>
  </div>
</div>

<div class="container mx-auto px-4 py-8">
  <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
    <% for (const event of events) { %>
      <div class="localpreneur-card text-center bg-white rounded-lg shadow-md overflow-hidden">
        <img src="/images/<%= event.images[0].filename %>" alt="<%= event.name %>" class="w-full h-48 object-cover">
        <div class="p-6">
          <h3 class="text-xl font-bold mb-4"><%= event.name %></h3>
          <p class="text-gray-600 mb-4"><%= event.description %></p>
          <a href="/events/<%= event._id %>" class="inline-block bg-blue-600 text-white px-4 py-2 rounded-md">
            Jelajahi <%= event.name %>
          </a>
        </div>
      </div>
    <% } %>
  </div>
</div>
