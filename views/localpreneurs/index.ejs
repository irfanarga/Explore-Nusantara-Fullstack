
<% layout('layouts/app') %>  

<div class="min-h-screen bg-gray-50">
<!-- Header -->
<div class="bg-blue-600 text-white py-4">
  <div class="container mx-auto flex justify-between items-center px-4">
    <a href="/localpreneurs" id="tombol-kembali" class="text-white flex items-center gap-2">
      &larr; Kembali ke Beranda
    </a>
    <h1 class="text-2xl font-bold">UMKM di Indonesia</h1>
  </div>
</div>

<div class="container mx-auto px-4 py-8">
  <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
    <% for (const localpreneur of localpreneurs) { %>
      <div class="localpreneur-card text-center bg-white rounded-lg shadow-md overflow-hidden">
        <img src="/images/<%= localpreneur.images[0].filename %>" alt="<%= localpreneur.name %>" class="w-full h-48 object-cover">
        <div class="p-6">
          <h3 class="text-xl font-bold mb-4"><%= localpreneur.name %></h3>
          <p class="text-gray-600 mb-4"><%= localpreneur.description %></p>
          <a href="/localpreneurs/<%= localpreneur._id %>" class="inline-block bg-blue-600 text-white px-4 py-2 rounded-md">
            Jelajahi <%= localpreneur.name %>
          </a>
        </div>
      </div>
    <% } %>
  </div>
</div>
